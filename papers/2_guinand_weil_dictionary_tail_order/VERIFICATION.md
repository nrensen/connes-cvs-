# Zero-Source Transport Verification Report

Last updated: 2026-08-12 (release-coherence audit, dependency and reproducibility
commands corrected; mathematical artifacts unchanged)

Package:

```text
A finite Guinand-Weil dictionary and archimedean tail order
for the truncated Weil quadratic form
```

## Body Boundary

The manuscript is a compact theorem paper with two theorem spines, one finite
quotient corollary, one support corollary, and one certification corollary:

1. exact finite Guinand-Weil zero-source dictionary (Theorem 2.5), with an
   entry-identification lemma (Lemma 2.1) pinning the source assembly to the
   CCM closed forms at equation level;
2. exact finite source quotient of dimension `2N+1`, now stated as an iff
   (Corollary 2.4);
3. positive-dimensional non-collapsing pole-neutral finite subfamily
   (Corollary 2.7), with the diagonal derivative identity now stated and
   proved;
4. finite-T archimedean tail order after the Galerkin band (Theorem 3.2),
   including strict observability and strict total positivity, upgraded to a
   two-sided finite certification rule with an explicit closed-form budget and
   the asymptotic `B_T = (2N+1) rho (log(T/2pi)+1)/(pi^2 T) (1+o(1))`
   (Corollary 3.3);
5. a self-contained envelope lemma (Lemma 3.1): `h_+'(t) <= 1/t + 13/(10 t^2)`
   and hence `h_+(t) <= log t - 8/5` for all `t >= 7`, given only the Arb
   interval value `h_+(7)`; this removes the earlier circular provenance of the
   log envelope.

The manuscript now contains a worked example (c=13, N=4) with a zero-side
table over the first 512 zeros of zeta, and two figures generated by
`make_figures.py`.

The paper does not claim RH, Weil positivity, a prime-location bound, a
next-prime theorem, a factoring result, or an inverse map from arbitrary
Guinand-Weil test functions to finite Galerkin vectors.

## Core Verification Commands

Run from this directory after installing `requirements.txt` (`sympy` for the
symbolic guards, `mpmath` for the numerical dictionary/asymptotic guards,
`python-flint` for the Arb scripts, and `matplotlib` only for
`figures/make_figures.py`):

```bash
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_exact_series_identity.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_kernel_span_rank.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_full_matrix_source_quotient.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_pole_neutral_survival.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_finite_dictionary.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_arch_tail_dt_bridge.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_zero_side.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_dictionary_threeroute.py 512
PYTHONDONTWRITEBYTECODE=1 python3 scripts/arch_tail_exact_asymptotic.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_arch_tail_order.py        # needs python-flint
PYTHONDONTWRITEBYTECODE=1 python3 scripts/arch_tail_budget.py              # needs python-flint
PYTHONDONTWRITEBYTECODE=1 python3 scripts/arch_tail_stress_ladder.py       # needs python-flint
PYTHONDONTWRITEBYTECODE=1 python3 scripts/arb_ldlt_certify.py --selftest --c 13 --N 8 --prec 300
```

Expected highlights:

```text
exact series identity audit passed
kernel span-rank audit passed
full-matrix source-quotient audit passed
pole-neutral survival audit passed
finite dictionary checks passed        (3 independent pole routes, <= ~1e-61)
archimedean dT bridge: symbolic residuals 0; direct max abs error ~9.5e-125
three-route dictionary check: |route1-route2| ~ 2e-10 (generic, c=29,N=6),
  ~1e-14 (pole-neutral), ~2e-15 (c=13,N=4); zero side to M=512
archimedean tail-order checks passed   (h_+(7) Arb interval; strict TP smoke)
archimedean tail budget: trace <= 0.8965687..., entry <= 3.1772e-3
arb_ldlt_certify selftest PASS; c=13,N=8: n_pos=17 n_neg=0 CERTIFIED
```

The headline cutoff-free certificate (c=100, N=200, 9000 bits, n_pos=401,
n_neg=0) is regenerated by

```bash
python3 scripts/arb_ldlt_certify.py --selftest --c 100 --N 200 --prec 9000 \
        --json-out artifacts/c100_N200_arb_ldlt_prec9000_provenance.json
```

and its provenance JSON ships with the package alongside the original log. The
archived 2026-07-02 run took about 15 minutes on one Apple M2 Max core under
Python 3.12.11 and `python-flint` 0.8.0; that is historical provenance, not a
cross-platform runtime guarantee.

Run `shasum -a 256 -c SHA256SUMS` before regenerating artifacts in place. The
headline command writes the current date and measured build/LDL timings into the
provenance JSON, so those volatile fields make a fresh successful artifact differ
from the archived checksum.

## Artifact Trace

- `exact_series_audit.json`: exact integer checks, single-frequency identity.
- `kernel_span_rank_audit.json`: exact Volterra-kernel span rank through N=30.
- `full_matrix_source_quotient_audit.json`: exact `2N+1` source-quotient rank
  checks through N=30.
- `pole_neutral_survival_audit.json`: exact symbolic checks over Q(b) for the
  pole-square factorization and moment-row independence (3311 pole-square
  entries; 18 moment-independence determinant cases; 45 tabulated instances of
  the dimension formula `dim = N-s-1`, checked as exact rank computations of
  the assembled row systems).
- `arch_tail_order_check.json`: certified `h_+` threshold, rank-two Cauchy
  divided-difference guard, strict-total-positivity smoke checks (N=2: 251
  minors; N=3: 3431 minors; zero bad minors).  Smoke checks are not proof
  substitutes; the theorem's proof is the Andreief/Cauchy argument.
- `arch_tail_dt_bridge_audit.json`: symbolic, direct-kernel, and
  finite-difference verification that the finite-T Cauchy tail density is the
  derivative of the archimedean source convention (includes the diagonal
  bridge now displayed in the manuscript proof).
- `arch_tail_budget_c100_N200_T800.json`, `arch_tail_stress_ladder.json`:
  Arb interval outputs for the displayed tail envelopes.  The final tail
  envelope `h_+(t) <= log t` is proved in manuscript Lemma 3.1 for all
  `t >= 7` (self-contained; no external asymptotic needed).
- `threeroute_c29N6_generic.json`, `threeroute_c29N6_poleneutral.json`,
  `threeroute_c13N4_package.json`: the three-route dictionary confirmation
  (closed-form CCM assembly vs source-side evaluation vs zero-side sums to
  M=512, with rank-two tail corrections carrying explicit remainder bounds).
  The c=13 route-1 value is the provenance of the SOURCE_TOTAL constant in
  `verify_zero_side.py`.
- `zeta_zeros_512_dps30.json`: cached ordinates of the first 512 zeros.
- `eigenflow_c13N4.json`: the monotone eigenvalue flow lambda_j(Q_T^tot) up to
  lambda_j(Q_inf) with the B_T sandwich at c=13, N=4 (Figure 2 data): strict
  order, within-budget, and monotonicity all hold on the full T ladder.
- `zero_side_values.json`: the original M<=64 zero-side confirmation.
- `c100_N200_arb_ldlt_prec9000.log` (+ `_provenance.json`): the cutoff-free
  Arb interval LDL certificate, n_pos=401, n_neg=0; generator
  `arb_ldlt_certify.py` ships in-package with a self-test that checks the Arb
  entries against an independent mpmath recomputation of the same closed forms,
  agreeing to a relative tolerance of 1e-60. That is an agreement test, not a
  strict ball-containment test: the mpmath mirror truncates its geometric sums
  near 1e-70, so containment is not the assertion being made.
- `fig_dictionary.pdf`, `fig_tailorder.pdf`: manuscript figures; generator
  `make_figures.py`.

## Current Gate Status

- PDF build: passed (pdflatex + bibtex, no warnings, no undefined references).
- Manuscript scans: no em dashes; no banned-superlative hits; en dashes for
  all name pairs.
- Bibliography: 29 entries, all cited; Arb, Karlin 1968, Andreief 1886 +
  Forrester 2019, Yoshida 1992 added in the 2026-07-02 revision; Suzuki
  2606.09096 and Chirre-Molero now compiled into the bibliography (they were
  present in main.bib but missing from the shipped .bbl before this pass).
- Core script gate: passed as a two-interpreter gate (a plain python3 for the
  sympy/mpmath scripts; a python-flint-equipped interpreter for the Arb
  scripts).
- Pole guard upgraded: the pole normalization is now checked by three
  genuinely independent routes (y-integral; two-sided xi-integral against
  e^{-pi xi}, which exercises the evenness step; the closed-form
  C_c beta^2 (row)^2 square), all to ~1e-61.  The earlier two-route version
  was tautological under a change of variables.
- Release mirrors and archives: rebuilt after the final package pass, then
  verified by checksum and extraction gates (see SHA256SUMS).
